function Description() {
  return (
    <h3>
      A digital clock is a JavaScript program that dynamically displays the
      current time in hours, minutes, and seconds, updating every second to
      reflect the real-time.
    </h3>
  );
}
export default Description;
